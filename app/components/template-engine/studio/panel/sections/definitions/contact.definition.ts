import type { SectionDefinition } from "./index";

export const contactDefinition: SectionDefinition = {
  type: "contact",
  label: "Contact",
  variants: [{ value: "A", label: "A (Standard)" }],
};
